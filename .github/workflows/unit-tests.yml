name: C++ Unit Tests

on:
  pull_request:
    branches: [ main, ai/unittest/ptrs ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake g++ libgtest-dev ninja-build
          sudo apt-get install -y qtbase5-dev qtbase5-dev-tools # for Qt5
          sudo apt-get install -y qt6-base-dev qt6-base-dev-tools # for Qt6
          sudo apt-get install -y libatomic1

      - name: Configure CMake
        run: cmake -S testqt/cpp_recall -B testqt/cpp_recall/build -DBUILD_TESTS=ON -DCMAKE_CXX_STANDARD=17 -G Ninja

      - name: Build
        run: cmake --build testqt/cpp_recall/build

      - name: Run unit tests
        run: cd testqt/cpp_recall/build && ctest --output-on-failure
